{
  "builds": [
    {
      "src": "config.toml",
      "use": "@vercel/static-build",
      "config": { "distDir": "public" }
    }
  ],
  "env": {
    "HUGO_VERSION": "0.115.1"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1/index.html" }
  ]
}